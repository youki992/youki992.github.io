name: Sync WeChat RSS to Articles

on:
  workflow_dispatch:
  schedule:
    - cron: '10 */6 * * *'

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Check RSS config
        id: cfg
        env:
          WECHAT_RSS_URL: ${{ secrets.WECHAT_RSS_URL }}
        run: |
          if [ -z "$WECHAT_RSS_URL" ]; then
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "WECHAT_RSS_URL missing, skip silently."
          else
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout
        if: steps.cfg.outputs.enabled == 'true'
        uses: actions/checkout@v4

      - name: Setup Python
        if: steps.cfg.outputs.enabled == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Sync RSS -> js/articles-data.js
        if: steps.cfg.outputs.enabled == 'true'
        env:
          WECHAT_RSS_URL: ${{ secrets.WECHAT_RSS_URL }}
        run: |
          python scripts/sync_rss_to_articles.py

      - name: Commit and push
        if: steps.cfg.outputs.enabled == 'true'
        run: |
          if git diff --quiet; then
            echo "No changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add js/articles-data.js
          git commit -m "chore: sync wechat articles from rss"
          git push
